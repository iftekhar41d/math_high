# Python
__pycache__/
*.pyc
.venv/
venv/

# Node
node_modules/
web/dist/

# Dev server logs
*.log

# Env / secrets
.env
*.local

# SQLite data
api/data/*.db
api/data/*.db-journal

# Lecture media (written at runtime by the storage abstraction; seeded per host)
api/data/media/

# OS / editor
.DS_Store
.vscode/

# Agent tooling
.agents/
.claude/
.scratch/
docs/
CONTEXT.md
skills-lock.json
specification.md
# NB: .github/ (deploy workflow) and CLAUDE.md (repo guidance) stay tracked.

# Local-only ops scripts (hardcode this deployment's VPS details)
deploy/check-vps-usage.sh
